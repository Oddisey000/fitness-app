import React from "react";

import AppBarComponent from "./app-bar/app-bar.component";

const Header = () => {
  return (
    <div id="app-header">
      <AppBarComponent />
    </div>
  );
};

export default Header;
